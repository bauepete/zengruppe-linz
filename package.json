{
  "name": "zengruppe-linz",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/content-image-cleanup.mjs && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "cleanup:content-images": "node scripts/content-image-cleanup.mjs",
    "cleanup:content-images:dry": "node scripts/content-image-cleanup.mjs --dry-run",
    "test": "node --test scripts/__tests__/*.test.mjs"
  },
  "dependencies": {
    "@astrojs/markdown-remark": "^7.2.2",
    "astro": "^7.1.6"
  }
}
