<<<<<<< HEAD:.github/workflows/deploy-development.yml
name: Deploy Development
=======
name: Deploy development branch to GitHub Pages
>>>>>>> 83cde52 (Setup deployment for development):.github/workflows/deploy.yml

on:
  push:
    branches: [development]
  workflow_dispatch:

env:
  DEPLOY_TARGET: development

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Build Astro site
        uses: withastro/action@v6

  deploy:
    needs: build
    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
