## Tasks

- [x] Add the `markdown-content-pages` capability delta spec aligned with current implementation behavior.
- [x] Confirm the spec text reflects current routing shape (dynamic slug route for configured slugs plus dedicated home route).
- [x] Confirm links are authored as standard Markdown and rendering-layer link policy remains template responsibility.
- [x] Implement external http/https link policy in the Astro markdown rendering layer so external links open in a new tab/window with safe `rel` attributes.
